# LFH_font

LFH is a custom Font Header for my custom OS that (currently) only supports bmp style fonts (so each character is a bmp), and in here i'll explain the header in \[more\] detail and add a python script for putting a bunch of bmp files (one for each letter to be added) into a LFH file \[maybe adding a script to convert a ttf to LFH too? idk yet\]
